name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  electron-build:
    name: Build Electron App
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Test
        run: npm test

      - name: Build renderer
        run: npm run build:renderer

      - name: Build electron
        run: npm run build:electron

  macos-build:
    name: Build macOS App
    runs-on: macos-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode.app/Contents/Developer

      - name: Check Swift version
        run: swift --version

      - name: Build macOS app
        run: |
          cd macos/ADBBuddyNotch
          swift build --configuration debug

      - name: Run tests
        run: |
          cd macos/ADBBuddyNotch
          swift test

      - name: Package and launch app
        run: macos/ADBBuddyNotch/package-app.sh ci
